apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ecommerce
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80           # Nginx default port
      targetPort: 80     # container port
      nodePort: 30080    # external port


